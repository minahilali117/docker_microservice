apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-management
  namespace: online-catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-management
  template:
    metadata:
      labels:
        app: catalog-management
    spec:
      containers:
        - name: catalog-management
          image: docker.io/ayaankhan17/catalog-management:sha-bf273eb3102e9525054e982b124c7fb38fdc8dad
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
          env:
            - name: PORT
              value: "8081"
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-management
  namespace: online-catalog
spec:
  selector:
    app: catalog-management
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 30081
  type: NodePort
