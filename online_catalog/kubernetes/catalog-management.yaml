apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-management
  namespace: online-catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-management
  template:
    metadata:
      labels:
        app: catalog-management
    spec:
      containers:
        - name: catalog-management
          image: docker.io/ayaankhan17/catalog-management:sha-cb7ce02e306d229e2fab1b0e64008cf0f35c8499
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
          env:
            - name: PORT
              value: "8081"
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-management
  namespace: online-catalog
spec:
  selector:
    app: catalog-management
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 30081
  type: NodePort
